# Sentry no Stronilead — Plano de Implementação (PR 1)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Dar visibilidade de erro em produção no front e nas 12 funções serverless, com uma tela de erro decente no lugar da tela branca, sem que dado pessoal de lead saia do sistema.

**Architecture:** A lógica de limpeza de dado sensível vive num módulo puro e sem dependências (`src/lib/sentryScrub.js`), usado pelos dois lados. O front chama `initSentry()` no `main.jsx` e liga os três callbacks de erro do React 19 no `createRoot`. O backend usa um `withSentry(handler)` em `api/_sentry.js`, nome com underscore que a Vercel não publica como função, mantendo o teto de 12 intacto. Tudo é gateado por variável de ambiente: sem DSN, nada inicializa.

**Tech Stack:** React 19, Vite 8, Vitest 4, `@sentry/react`, `@sentry/node`, `@sentry/vite-plugin`, funções serverless da Vercel.

**Spec:** `docs/superpowers/specs/2026-08-03-seguranca-appcheck-sentry-design.md`

**Baseline:** 560 testes verdes em 23 arquivos (`npm test`). Precisa continuar assim.

---

## Estrutura de arquivos

| Arquivo | Responsabilidade |
|---|---|
| `src/lib/sentryScrub.js` | **Novo.** Funções puras: mascarar dado pessoal, detectar ruído, limpar o evento. Zero dependências, roda em node e no browser. |
| `src/lib/__tests__/sentryScrub.test.js` | **Novo.** Testes do módulo acima. |
| `src/lib/sentry.js` | **Novo.** Init do front, contexto de usuário. Único arquivo do front que importa `@sentry/react`. |
| `src/components/ErrorBoundary.jsx` | **Novo.** Tela de erro na identidade do app. |
| `src/main.jsx` | Chama `initSentry()`; liga os callbacks de erro do React 19. |
| `src/App.jsx` | Um `useEffect` que sincroniza o contexto do usuário; `<AppErrorBoundary>` em volta da área de conteúdo. |
| `vite.config.js` | Source map no build, envio ao Sentry, `release` a partir do commit da Vercel. |
| `api/_sentry.js` | **Novo.** `withSentry(handler)` com flush antes do return. |
| `api/*.js` (12 arquivos) | Envolvem o handler com `withSentry`. |
| `.env.example` | Chaves novas do Sentry. |

**Por que a limpeza mora num arquivo separado do init:** os testes rodam em node, sem browser. Se a lógica testável estivesse junto do `import * as Sentry from '@sentry/react'`, o teste carregaria o SDK do browser sem necessidade. Separando, o teste importa um arquivo sem dependência nenhuma. É também o arquivo que o backend importa, então ele não pode arrastar código de browser junto.

---

## Task 1: Instalar as dependências

**Files:**
- Modify: `package.json`

- [ ] **Step 1: Instalar**

```bash
npm install @sentry/react @sentry/node
```

```bash
npm install -D @sentry/vite-plugin
```

- [ ] **Step 2: Conferir que a suíte segue verde**

Run: `npm test`
Expected: `Tests  560 passed (560)`

- [ ] **Step 3: Commit**

```bash
git add package.json package-lock.json && git commit -m "chore: adiciona dependencias do Sentry"
```

---

## Task 2: Mascaramento de dado pessoal

O coração da promessa de LGPD. É lógica pura, então vai por teste primeiro.

**Files:**
- Create: `src/lib/sentryScrub.js`
- Test: `src/lib/__tests__/sentryScrub.test.js`

- [ ] **Step 1: Escrever o teste que falha**

Criar `src/lib/__tests__/sentryScrub.test.js`:

```js
import { describe, it, expect } from 'vitest';
import { maskSensitive, scrubDeep } from '../sentryScrub.js';

describe('maskSensitive', () => {
  it('mascara CPF formatado', () => {
    expect(maskSensitive('cliente 123.456.789-01 nao encontrado'))
      .toBe('cliente [cpf] nao encontrado');
  });

  it('mascara CPF sem pontuacao', () => {
    expect(maskSensitive('doc 12345678901 invalido'))
      .toBe('doc [documento] invalido');
  });

  it('mascara e-mail', () => {
    expect(maskSensitive('falha para joao.silva@academia.com.br'))
      .toBe('falha para [email]');
  });

  it('mascara telefone com DDD e nono digito', () => {
    expect(maskSensitive('whatsapp (11) 98765-4321 recusado'))
      .toBe('whatsapp [telefone] recusado');
  });

  it('mascara telefone sem formatacao', () => {
    expect(maskSensitive('tel 11987654321')).toBe('tel [telefone]');
  });

  it('nao mascara timestamp de 13 digitos', () => {
    expect(maskSensitive('expiresAt 1754246400000')).toBe('expiresAt 1754246400000');
  });

  it('deixa texto sem dado pessoal intacto', () => {
    const msg = 'TypeError: cannot read property status of undefined';
    expect(maskSensitive(msg)).toBe(msg);
  });

  it('devolve valor nao-string sem alterar', () => {
    expect(maskSensitive(42)).toBe(42);
    expect(maskSensitive(null)).toBe(null);
  });
});

describe('scrubDeep', () => {
  it('percorre objeto aninhado', () => {
    const input = { lead: { nome: 'Ana', email: 'ana@x.com' }, ok: true };
    expect(scrubDeep(input)).toEqual({ lead: { nome: 'Ana', email: '[email]' }, ok: true });
  });

  it('percorre array', () => {
    expect(scrubDeep(['a@b.com', 'texto'])).toEqual(['[email]', 'texto']);
  });

  it('preserva numeros e booleanos', () => {
    expect(scrubDeep({ n: 7, b: false })).toEqual({ n: 7, b: false });
  });

  it('para na profundidade maxima sem estourar a pilha', () => {
    const deep = { a: { b: { c: { d: { e: { f: { g: 'x@y.com' } } } } } } };
    expect(() => scrubDeep(deep)).not.toThrow();
  });
});
```

Nota sobre o segundo teste: um CPF cru **não** casa como telefone, porque o padrão de celular exige o `9` na terceira posição e um CPF raramente o tem ali. Por isso ele cai numa regra própria, de 11 dígitos soltos, com o rótulo `[documento]`. O que importa nos dois casos é que o número não sai.

- [ ] **Step 2: Rodar e ver falhar**

Run: `npx vitest run src/lib/__tests__/sentryScrub.test.js`
Expected: FAIL, `Failed to resolve import "../sentryScrub.js"`

- [ ] **Step 3: Implementar**

Criar `src/lib/sentryScrub.js`:

```js
// Limpeza de dado pessoal antes de qualquer evento sair para o Sentry.
// Módulo PURO e sem dependências de propósito: roda no browser (front) e em
// node (funções da api/), e os testes o importam sem carregar SDK nenhum.
//
// A ordem dos padrões importa. CPF formatado primeiro, porque a pontuação o
// torna inconfundível. E-mail antes de telefone, para que a sequência de
// dígitos dentro de um endereço não seja comida pelo padrão de telefone.
// As âncoras \b nas pontas do padrão de telefone evitam que ele morda o meio
// de um número longo, como um timestamp em milissegundos.

const PATTERNS = [
  [/\b\d{3}\.\d{3}\.\d{3}-\d{2}\b/g, '[cpf]'],
  [/\b[\w.+-]+@[\w-]+\.[\w.-]+\b/g, '[email]'],
  [/\b(?:\+?55[\s-]?)?\(?\d{2}\)?[\s-]?9?\d{4}[\s-]?\d{4}\b/g, '[telefone]'],
];

const MAX_DEPTH = 6;

export function maskSensitive(value) {
  if (typeof value !== 'string') return value;
  let out = value;
  for (const [re, label] of PATTERNS) out = out.replace(re, label);
  return out;
}

// Percorre estrutura aninhada aplicando o mascaramento em toda string.
// O teto de profundidade evita laço infinito em objeto com referência
// circular, que aparece de vez em quando em payload de erro.
export function scrubDeep(value, depth = 0) {
  if (depth >= MAX_DEPTH) return undefined;
  if (typeof value === 'string') return maskSensitive(value);
  if (Array.isArray(value)) return value.map((v) => scrubDeep(v, depth + 1));
  if (value && typeof value === 'object') {
    const out = {};
    for (const [k, v] of Object.entries(value)) out[k] = scrubDeep(v, depth + 1);
    return out;
  }
  return value;
}
```

- [ ] **Step 4: Rodar e ver passar**

Run: `npx vitest run src/lib/__tests__/sentryScrub.test.js`
Expected: PASS, 12 testes

- [ ] **Step 5: Commit**

```bash
git add src/lib/sentryScrub.js src/lib/__tests__/sentryScrub.test.js && git commit -m "feat: mascaramento de dado pessoal para eventos do Sentry"
```

---

## Task 3: Filtro de ruído e limpeza do evento

**Files:**
- Modify: `src/lib/sentryScrub.js`
- Modify: `src/lib/__tests__/sentryScrub.test.js`

- [ ] **Step 1: Escrever o teste que falha**

Trocar a primeira linha de import do arquivo de teste por:

```js
import { maskSensitive, scrubDeep, isNoise, scrubEvent } from '../sentryScrub.js';
```

E acrescentar ao final do arquivo:

```js
describe('isNoise', () => {
  it('descarta ResizeObserver loop', () => {
    expect(isNoise({ exception: { values: [{ value: 'ResizeObserver loop limit exceeded' }] } })).toBe(true);
  });

  it('descarta erro vindo de extensao do navegador', () => {
    const event = {
      exception: {
        values: [{
          value: 'boom',
          stacktrace: { frames: [{ filename: 'chrome-extension://abc/inject.js' }] },
        }],
      },
    };
    expect(isNoise(event)).toBe(true);
  });

  it('mantem erro normal do app', () => {
    const event = {
      exception: {
        values: [{
          value: 'TypeError: x is undefined',
          stacktrace: { frames: [{ filename: 'https://app.stronilead.com.br/assets/index.js' }] },
        }],
      },
    };
    expect(isNoise(event)).toBe(false);
  });

  it('mantem evento sem excecao', () => {
    expect(isNoise({ message: 'log qualquer' })).toBe(false);
  });
});

describe('scrubEvent', () => {
  it('mascara a mensagem da excecao', () => {
    const event = { exception: { values: [{ value: 'lead ana@x.com falhou' }] } };
    expect(scrubEvent(event).exception.values[0].value).toBe('lead [email] falhou');
  });

  it('mascara a mensagem solta', () => {
    expect(scrubEvent({ message: 'tel 11987654321' }).message).toBe('tel [telefone]');
  });

  it('remove o corpo da requisicao', () => {
    const event = { request: { url: '/api/plans', data: { cpf: '123.456.789-01' } } };
    const out = scrubEvent(event);
    expect(out.request.data).toBeUndefined();
    expect(out.request.url).toBe('/api/plans');
  });

  it('mascara breadcrumbs', () => {
    const event = { breadcrumbs: [{ message: 'buscou joao@x.com' }] };
    expect(scrubEvent(event).breadcrumbs[0].message).toBe('buscou [email]');
  });

  it('mascara o bloco extra', () => {
    const event = { extra: { payload: { email: 'a@b.com' } } };
    expect(scrubEvent(event).extra.payload.email).toBe('[email]');
  });

  it('devolve null para evento de ruido', () => {
    expect(scrubEvent({ exception: { values: [{ value: 'ResizeObserver loop' }] } })).toBe(null);
  });
});
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `npx vitest run src/lib/__tests__/sentryScrub.test.js`
Expected: FAIL, `isNoise is not a function`

- [ ] **Step 3: Implementar**

Acrescentar ao final de `src/lib/sentryScrub.js`:

```js
// Ruído conhecido: erro que não é bug nosso e só queima cota do plano gratuito.
const NOISE_MESSAGES = [
  /ResizeObserver loop/i,
  /Non-Error promise rejection captured/i,
  /Extension context invalidated/i,
];

const NOISE_FILES = [
  /^chrome-extension:\/\//,
  /^moz-extension:\/\//,
  /^safari-web-extension:\/\//,
];

export function isNoise(event) {
  const values = event?.exception?.values;
  if (!Array.isArray(values) || values.length === 0) return false;

  for (const entry of values) {
    const message = String(entry?.value || '');
    if (NOISE_MESSAGES.some((re) => re.test(message))) return true;

    // O último frame é onde o erro nasceu. Se veio de extensão, não é nosso.
    const frames = entry?.stacktrace?.frames;
    if (Array.isArray(frames) && frames.length > 0) {
      const origin = String(frames[frames.length - 1]?.filename || '');
      if (NOISE_FILES.some((re) => re.test(origin))) return true;
    }
  }
  return false;
}

// beforeSend do Sentry: devolver null descarta o evento.
export function scrubEvent(event) {
  if (!event) return event;
  if (isNoise(event)) return null;

  if (typeof event.message === 'string') event.message = maskSensitive(event.message);

  if (Array.isArray(event.exception?.values)) {
    for (const entry of event.exception.values) {
      if (typeof entry.value === 'string') entry.value = maskSensitive(entry.value);
    }
  }

  if (Array.isArray(event.breadcrumbs)) {
    for (const crumb of event.breadcrumbs) {
      if (typeof crumb.message === 'string') crumb.message = maskSensitive(crumb.message);
      if (crumb.data) crumb.data = scrubDeep(crumb.data);
    }
  }

  if (event.extra) event.extra = scrubDeep(event.extra);

  // O corpo da requisição é onde dado de lead trafega no backend. Ele não tem
  // valor de diagnóstico que justifique o risco, então sai inteiro.
  if (event.request) {
    delete event.request.data;
    delete event.request.cookies;
    if (event.request.headers) {
      delete event.request.headers.authorization;
      delete event.request.headers.cookie;
    }
  }

  return event;
}
```

- [ ] **Step 4: Rodar e ver passar**

Run: `npx vitest run src/lib/__tests__/sentryScrub.test.js`
Expected: PASS, 22 testes

- [ ] **Step 5: Rodar a suíte inteira**

Run: `npm test`
Expected: `Tests  582 passed (582)`

- [ ] **Step 6: Commit**

```bash
git add src/lib/sentryScrub.js src/lib/__tests__/sentryScrub.test.js && git commit -m "feat: filtro de ruido e limpeza de evento do Sentry"
```

---

## Task 4: Init do Sentry no front

**Files:**
- Create: `src/lib/sentry.js`

Não há teste automatizado aqui: o arquivo é fiação com o SDK, e a lógica que merece teste já ficou nas Tasks 2 e 3. A verificação é ao vivo, na Task 11.

- [ ] **Step 1: Implementar**

Criar `src/lib/sentry.js`:

```js
// Fiação do Sentry no front. Único arquivo do front que importa o SDK.
// A lógica de limpeza mora em sentryScrub.js, que é puro e testado.

import * as Sentry from '@sentry/react';
import { scrubEvent } from './sentryScrub.js';

const env = import.meta.env || {};
const DSN = env.VITE_SENTRY_DSN || '';

// Sem DSN o Sentry não sobe. É o botão de desligar: basta remover a variável
// no painel da Vercel e redeployar, sem tocar em código. Também mantém o
// `npm run dev` mudo por padrão.
export function initSentry() {
  if (!DSN) return false;

  Sentry.init({
    dsn: DSN,
    environment: env.VITE_SENTRY_ENVIRONMENT || 'production',
    release: env.VITE_APP_RELEASE || undefined,

    // Não anexa IP nem dado de usuário automaticamente. O que vai para o
    // Sentry é só o que setSentryUser manda, de propósito.
    sendDefaultPii: false,

    // Session Replay fica de fora. Ele grava a tela, e a tela tem ficha de
    // lead aberta. Decisão de privacidade, não de esforço.
    integrations: [Sentry.browserTracingIntegration()],
    tracesSampleRate: 0.1,

    beforeSend: scrubEvent,
  });

  return true;
}

// Só uid, tenantId e role. Nome e e-mail ficam no nosso banco; o cruzamento
// com a pessoa é feito lá, não no Sentry.
export function setSentryUser({ uid, tenantId, role, impersonating }) {
  if (!DSN) return;
  Sentry.setUser(uid ? { id: uid } : null);
  Sentry.setTags({
    tenant: tenantId || 'sem-tenant',
    role: role || 'desconhecido',
    impersonating: impersonating ? 'sim' : 'nao',
  });
}

export function clearSentryUser() {
  if (!DSN) return;
  Sentry.setUser(null);
  Sentry.setTags({ tenant: 'sem-tenant', role: 'desconhecido', impersonating: 'nao' });
}
```

- [ ] **Step 2: Conferir que o build passa**

Run: `npm run build`
Expected: build conclui sem erro

- [ ] **Step 3: Commit**

```bash
git add src/lib/sentry.js && git commit -m "feat: init do Sentry no front, gateado por DSN"
```

---

## Task 5: Ligar o Sentry no main.jsx

**Files:**
- Modify: `src/main.jsx`

- [ ] **Step 1: Substituir o arquivo inteiro**

O `main.jsx` atual tem 10 linhas. Substituir o conteúdo por:

```jsx
import React from 'react'
import ReactDOM from 'react-dom/client'
import * as Sentry from '@sentry/react'
import { initSentry } from './lib/sentry.js'
import App from './App.jsx'
import './index.css'

// Antes do render, para que erro durante o boot da árvore seja capturado.
initSentry()

// React 19 expõe três ganchos de erro no createRoot. Sem eles, erro capturado
// por ErrorBoundary não chega ao Sentry, porque o React o considera tratado.
ReactDOM.createRoot(document.getElementById('root'), {
  onUncaughtError: Sentry.reactErrorHandler(),
  onCaughtError: Sentry.reactErrorHandler(),
  onRecoverableError: Sentry.reactErrorHandler(),
}).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)
```

- [ ] **Step 2: Conferir build e lint**

Run: `npm run build && npm run lint`
Expected: build conclui; o lint não deve ganhar erro novo (a base já tem 15 erros conhecidos de `react-hooks` v7)

- [ ] **Step 3: Commit**

```bash
git add src/main.jsx && git commit -m "feat: liga o Sentry aos ganchos de erro do React 19"
```

---

## Task 6: ErrorBoundary com tela de erro

**Files:**
- Create: `src/components/ErrorBoundary.jsx`

- [ ] **Step 1: Implementar**

Criar `src/components/ErrorBoundary.jsx`:

```jsx
import * as Sentry from '@sentry/react';
import { AlertTriangle, RotateCw } from 'lucide-react';
import { SurgeMark, StronileadWordmark } from './brand/SurgeMark.jsx';

// Tela mostrada quando uma view quebra. Substitui a tela branca de hoje.
// O código do evento aparece para o usuário poder passar ao suporte, o que
// transforma "deu erro" em algo rastreável.
function ErrorFallback({ resetError, eventId }) {
  return (
    <div className="grid place-items-center h-full py-24 px-6">
      <div className="w-full max-w-[420px] text-center">
        <div className="flex items-center justify-center gap-2.5 mb-6">
          <SurgeMark size={22} />
          <StronileadWordmark className="text-[16px]" />
        </div>

        <span className="inline-grid place-items-center size-12 rounded-2xl bg-rose-50 dark:bg-rose-500/10 text-rose-600 dark:text-rose-400 mb-4">
          <AlertTriangle className="size-6" />
        </span>

        <h2 className="font-display text-[20px] font-semibold tracking-tight">
          Essa tela travou
        </h2>
        <p className="mt-2 text-[13.5px] text-gray-500 dark:text-neutral-400 leading-relaxed">
          O resto do sistema segue funcionando. Já fomos avisados e estamos olhando.
        </p>

        <button
          type="button"
          onClick={resetError}
          className="mt-6 h-11 px-5 rounded-xl bg-brand-600 hover:bg-brand-700 text-white text-[13.5px] font-semibold inline-flex items-center justify-center gap-2 transition active:scale-[.99]"
        >
          <RotateCw className="size-4" /> Tentar de novo
        </button>

        {eventId && (
          <p className="mt-5 text-[11.5px] text-gray-400 dark:text-neutral-500">
            Código do erro: <span className="font-mono">{eventId}</span>
          </p>
        )}
      </div>
    </div>
  );
}

// Funciona mesmo sem DSN configurado: o Sentry.ErrorBoundary continua
// capturando e renderizando o fallback, só não envia nada.
export function AppErrorBoundary({ children }) {
  return (
    <Sentry.ErrorBoundary fallback={(props) => <ErrorFallback {...props} />}>
      {children}
    </Sentry.ErrorBoundary>
  );
}
```

- [ ] **Step 2: Conferir build**

Run: `npm run build`
Expected: build conclui sem erro

- [ ] **Step 3: Commit**

```bash
git add src/components/ErrorBoundary.jsx && git commit -m "feat: tela de erro no lugar da tela branca"
```

---

## Task 7: Contexto do usuário e ErrorBoundary no App.jsx

**Files:**
- Modify: `src/App.jsx` (imports no topo, um `useEffect` novo perto da linha 322, e a área de conteúdo nas linhas 1303 e 1356)

- [ ] **Step 1: Adicionar os imports**

No bloco de imports do topo de `src/App.jsx`, acrescentar:

```jsx
import { AppErrorBoundary } from './components/ErrorBoundary.jsx';
import { setSentryUser, clearSentryUser } from './lib/sentry.js';
```

- [ ] **Step 2: Adicionar o effect de contexto**

Há **quatro** chamadas de `setAppUser({...})` no arquivo (linhas 437, 459, 482, 504) mais os caminhos que passam `null` (linhas 329, 367, 519, 524). Instrumentar cada uma seria oito pontos para manter em sincronia. Em vez disso, um único effect que observa o estado já resolvido.

Inserir logo **antes** do comentário `// 1. Inicialização Auth e Persistência de Sessão` (linha 323), depois da declaração de estado da linha 321:

```jsx
  // Contexto do Sentry: qual academia e qual papel, para saber quem está
  // sofrendo sem mandar nome nem e-mail para fora. Um effect só, em vez de
  // instrumentar os oito pontos que mexem em appUser.
  useEffect(() => {
    if (appUser) {
      setSentryUser({
        uid: firebaseUser?.uid,
        tenantId: appUser.tenantId,
        role: appUser.role,
        impersonating: !!appUser.impersonating,
      });
    } else {
      clearSentryUser();
    }
  }, [appUser, firebaseUser?.uid]);
```

- [ ] **Step 3: Envolver a área de conteúdo**

Na linha 1303 está a `div` de conteúdo, que fecha na 1356. Envolver **o conteúdo dela**, não a `div`, para que a barra lateral e o cabeçalho sobrevivam a uma quebra de view.

Abertura, antes:

```jsx
        <div className="flex-1 overflow-x-hidden overflow-y-auto p-4 md:p-8 relative custom-scrollbar">
          {appUser.superAdminOnly ? (
```

Abertura, depois:

```jsx
        <div className="flex-1 overflow-x-hidden overflow-y-auto p-4 md:p-8 relative custom-scrollbar">
          <AppErrorBoundary>
          {appUser.superAdminOnly ? (
```

Fechamento, antes:

```jsx
            </div>
          )}
        </div>
      </main>
```

Fechamento, depois:

```jsx
            </div>
          )}
          </AppErrorBoundary>
        </div>
      </main>
```

- [ ] **Step 4: Conferir build, lint e testes**

Run: `npm run build && npm run lint && npm test`
Expected: build conclui; lint sem erro novo; `Tests  582 passed (582)`

- [ ] **Step 5: Commit**

```bash
git add src/App.jsx && git commit -m "feat: contexto de usuario no Sentry e ErrorBoundary na area de conteudo"
```

---

## Task 8: Source maps no build

Sem isso o stack trace chega minificado e é inútil para achar a linha.

**Files:**
- Modify: `vite.config.js`

- [ ] **Step 1: Acrescentar o import no topo**

```js
import { sentryVitePlugin } from '@sentry/vite-plugin'
```

- [ ] **Step 2: Trocar o bloco `plugins` e acrescentar `define`**

Substituir `plugins: [react()],` por:

```js
  plugins: [
    react(),
    // Só entra quando o token existe. Em build local e em fork sem segredo,
    // o plugin nem carrega e o build segue normal.
    ...(process.env.SENTRY_AUTH_TOKEN
      ? [sentryVitePlugin({
          org: process.env.SENTRY_ORG,
          project: process.env.SENTRY_PROJECT,
          authToken: process.env.SENTRY_AUTH_TOKEN,
          // Sobe o mapa e apaga do dist. Sem isso o código-fonte fica
          // público em produção.
          sourcemaps: { filesToDeleteAfterUpload: ['./dist/**/*.map'] },
        })]
      : []),
  ],
  define: {
    // Identifica qual deploy gerou o erro.
    'import.meta.env.VITE_APP_RELEASE': JSON.stringify(
      process.env.VERCEL_GIT_COMMIT_SHA || 'dev'
    ),
  },
```

- [ ] **Step 3: Acrescentar `sourcemap` ao bloco `build`**

O bloco `build` já existe com `rollupOptions`. Acrescentar `sourcemap: true` como primeira chave, mantendo o `rollupOptions` e o `manualChunks` atuais intactos:

```js
  build: {
    sourcemap: true,
    rollupOptions: {
      // conteúdo atual permanece exatamente igual
    },
  },
```

- [ ] **Step 4: Conferir que o build local segue funcionando sem token**

Run: `npm run build && ls dist/assets/*.map | head -3`
Expected: build conclui; arquivos `.map` existem (local, sem token, o plugin não roda e o mapa fica no disco)

- [ ] **Step 5: Commit**

```bash
git add vite.config.js && git commit -m "feat: envia source map ao Sentry e marca o release pelo commit"
```

---

## Task 9: Wrapper das funções serverless

**Files:**
- Create: `api/_sentry.js`
- Modify: os 12 arquivos de `api/` sem underscore

- [ ] **Step 1: Criar o wrapper**

Criar `api/_sentry.js`:

```js
import * as Sentry from '@sentry/node';
import { scrubEvent } from '../src/lib/sentryScrub.js';

// Captura de erro nas funções serverless. O arquivo começa com underscore, e
// a Vercel não publica esses como função — o teto de 12 do plano Hobby fica
// intacto.

const DSN = process.env.SENTRY_DSN || '';
let started = false;

function start() {
  if (started || !DSN) return;
  Sentry.init({
    dsn: DSN,
    environment: process.env.VERCEL_ENV || 'production',
    release: process.env.VERCEL_GIT_COMMIT_SHA || undefined,
    sendDefaultPii: false,
    // Sem tracing no backend: custo de cold start sem ganho, o objetivo aqui
    // é erro, não performance.
    tracesSampleRate: 0,
    beforeSend: scrubEvent,
  });
  started = true;
}

// Envolve um handler da Vercel. Repassa o erro depois de capturar, para o
// comportamento de resposta continuar exatamente o de hoje.
export function withSentry(handler) {
  return async function sentryWrapped(req, res) {
    start();
    try {
      return await handler(req, res);
    } catch (err) {
      if (DSN) {
        Sentry.captureException(err, { tags: { endpoint: String(req?.url || '') } });
        // A função congela assim que responde. Sem flush explícito o evento
        // morre no buffer e nunca chega ao Sentry.
        await Sentry.flush(2000).catch(() => {});
      }
      throw err;
    }
  };
}
```

- [ ] **Step 2: Aplicar nos 12 handlers**

Todos têm a mesma assinatura, `export default async function handler(req, res) {`. Em cada arquivo, três edições.

Import a acrescentar no topo:

```js
import { withSentry } from './_sentry.js';
```

Abertura, de:

```js
export default async function handler(req, res) {
```

para:

```js
export default withSentry(async function handler(req, res) {
```

Fechamento, da chave que fecha o handler:

```js
}
```

para:

```js
});
```

Arquivos e a linha onde o handler abre:

| Arquivo | Linha de abertura |
|---|---|
| `api/admin-set-password.js` | 4 |
| `api/admin-create-user.js` | 6 |
| `api/admin-delete-user.js` | 10 |
| `api/tenant-resolve.js` | 15 |
| `api/invite-create.js` | 19 |
| `api/invite-accept.js` | 22 |
| `api/tenant-status.js` | 64 |
| `api/provision-tenant.js` | 66 |
| `api/impersonate.js` | 72 |
| `api/plans.js` | 73 |
| `api/super-overview.js` | 90 |
| `api/asaas.js` | 449 |

Atenção: a chave a trocar é a que fecha a **função handler**, não necessariamente a última do arquivo. Conferir arquivo por arquivo.

- [ ] **Step 3: Conferir que nada quebrou sintaticamente**

Run: `npm run lint`
Expected: sem erro novo de sintaxe nos arquivos de `api/`

- [ ] **Step 4: Commit**

```bash
git add api/ && git commit -m "feat: captura de erro nas 12 funcoes serverless"
```

---

## Task 10: Variáveis de ambiente

**Files:**
- Modify: `.env.example`

- [ ] **Step 1: Acrescentar ao final do `.env.example`**

```
# ---- Sentry (monitoramento de erro) --------------------------------
# Sem VITE_SENTRY_DSN o Sentry NÃO inicializa e o app sobe igual a hoje.
# É o botão de desligar: remova a variável na Vercel e redeploye.
VITE_SENTRY_DSN=
VITE_SENTRY_ENVIRONMENT=production

# Backend (funções api/). DSN do MESMO projeto no Sentry.
SENTRY_DSN=

# Só no build, para subir o source map e o stack trace vir legível.
# Sem o token o plugin nem carrega e o build segue normal.
SENTRY_AUTH_TOKEN=
SENTRY_ORG=
SENTRY_PROJECT=
```

- [ ] **Step 2: Commit**

```bash
git add .env.example && git commit -m "docs: variaveis de ambiente do Sentry"
```

---

## Task 11: Verificação ao vivo

Nada aqui é automatizável. É a prova de que a promessa de LGPD se sustenta na prática.

- [ ] **Step 1: Criar o projeto no Sentry**

Criar organização e projeto do tipo React em sentry.io, plano gratuito. Guardar o DSN.

- [ ] **Step 2: Configurar as variáveis na Vercel**

Em Project Settings, Environment Variables, cadastrar as seis chaves da Task 10. `VITE_SENTRY_ENVIRONMENT` deve ser `preview` no ambiente de preview e `production` em produção.

- [ ] **Step 3: Confirmar que a função serverless enxerga o `src/`**

`api/_sentry.js` importa de `../src/lib/sentryScrub.js`. Nenhum arquivo de `api/` importava de `src/` antes deste PR, então é padrão novo e precisa de prova no deploy real, não só local.

No deploy de preview do PR, chamar `/api/tenant-resolve?slug=stronix` e confirmar que responde normalmente.

Se o build da Vercel falhar com erro de módulo não encontrado, a saída é copiar `sentryScrub.js` para `api/_scrub.js` e importar de lá, aceitando a duplicação. Nesse caso, acrescentar em ambos os arquivos um comentário avisando que precisam andar juntos, e apontar o teste para os dois.

- [ ] **Step 4: Disparar um erro de propósito e conferir o que chegou**

No deploy de preview, logado, rodar no console do navegador:

```js
setTimeout(() => { throw new Error('teste sentry lead joao@teste.com cpf 123.456.789-01 tel 11987654321'); }, 0)
```

No painel do Sentry, o evento precisa mostrar:
- a mensagem com `[email]`, `[cpf]` e `[telefone]` no lugar dos valores
- a tag `tenant` com o tenantId certo, e a tag `role`
- **nenhum** nome ou e-mail de usuário no bloco de contexto
- stack trace legível, com nome de arquivo e linha do fonte

- [ ] **Step 5: Conferir a tela de erro**

Provocar uma quebra numa view e confirmar que a barra lateral e o cabeçalho continuam de pé, que a tela mostra o código do erro, e que "Tentar de novo" recupera.

- [ ] **Step 6: Abrir o PR**

```bash
git push -u origin claude/cloudflare-sentry-security-63fe6e
```

---

## Self-review deste plano

**Cobertura do spec:** as sete peças do PR 1 no spec (`sentry.js`, `main.jsx`, `ErrorBoundary.jsx`, `App.jsx`, `vite.config.js`, `api/_sentry.js`, `.env.example`) têm task correspondente. A tabela de configuração do spec está na Task 4. As três obrigações de privacidade (mascarar, descartar corpo, filtrar ruído) estão nas Tasks 2 e 3. Os testes que o spec pede estão nas Tasks 2, 3 e 11.

**Divergência assumida em relação ao spec:** o spec previa um teste unitário para o gating do init sem DSN. Ele foi retirado. Testar isso exigiria importar `sentry.js`, que carrega o SDK de browser numa suíte que roda em node, e o retorno seria só confirmar um `if`. O gating é verificado na Task 11, quando o app roda sem DSN em ambiente local.

**Consistência de nomes:** `maskSensitive`, `scrubDeep`, `isNoise` e `scrubEvent` são definidos nas Tasks 2 e 3 e usados com esses mesmos nomes nas Tasks 4 e 9. `initSentry`, `setSentryUser` e `clearSentryUser` vêm da Task 4 e são usados nas Tasks 5 e 7. `AppErrorBoundary` vem da Task 6 e é usado na 7. `withSentry` vem da Task 9.

**Contagem de testes:** 560 na base, mais 12 na Task 2, mais 10 na Task 3, dá 582. Esse número aparece nas Tasks 3 e 7.
